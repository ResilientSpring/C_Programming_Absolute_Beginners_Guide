#include "invStruct.h"
#include <stdlib.h>

int main() {

	struct invStruct items[500];

	struct invStruct* item1;
	struct invStruct* item2;
	struct invStruct* item3;
	
	item1 = (struct invStruct*) malloc(sizeof(struct invStruct));

	item2 = (struct invStruct*) malloc(sizeof(invStruct));

}